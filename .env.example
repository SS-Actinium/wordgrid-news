# World Grid — environment template
# Copy to .env.local for development. Never commit real secrets.

# --- Auth (required in production) ---
# Min 12 chars, not "admin123"
ADMIN_PASSWORD=
# Min 32 random chars, independent of password
ADMIN_SECRET=

# --- Cron / RSS sync (required in production for /api/news/sync) ---
CRON_SECRET=

# --- Optional AI keys (or set via Admin → AI Keys) ---
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
XAI_API_KEY=
# GEMINI_IMAGE_MODEL=gemini-2.0-flash-preview-image-generation

# --- Site ---
# NEXT_PUBLIC_SITE_URL=https://wordgrid.news
